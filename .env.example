# Copie este arquivo para ".env" e preencha com os dados reais do seu MySQL.
# NUNCA suba o arquivo ".env" de verdade para o repositório (ele já está no .gitignore).

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=loja

JWT_SECRET=troque-este-segredo-por-uma-string-grande-e-aleatoria
